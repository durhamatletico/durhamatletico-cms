dependencies:
  post:
    - cd themes/atletico; bundle install
test:
  override:
    - cd themes/atletico; bundle exec scss-lint . -e stylesheets/styles.css,sass/lib/*
machine:
  php:
    version: 5.5.21